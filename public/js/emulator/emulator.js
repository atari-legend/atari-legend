
document.addEventListener('DOMContentLoaded', () => {
/*
    const loadingStatus = PCEJSUtil.loadingStatus(document.querySelector('.loading-status'));

    PCEJSAtariST({
        locateFile: function(path, scriptDirectory) {
            console.log('locatefile!', path, scriptDirectory);
            return '/js/pcejs-atarist/pce-atarist.wasm';
        },
        'arguments': ['-c', 'pce-config.cfg', '-r'],
        autoloadFiles: [
            '/js/emulator/tos-1.00-us.rom',
            '/js/emulator/pce-config.cfg',
            '/js/emulator/PP013_5A.st',
        ],
        print: console.log.bind(console),
        printErr: console.warn.bind(console),
        canvas: document.querySelector('.emulator-canvas'),
        monitorRunDependencies: function (remainingDependencies) {
            loadingStatus.update(remainingDependencies);
        },
    });
    */
});